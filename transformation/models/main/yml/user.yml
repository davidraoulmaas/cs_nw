version: 2

models:
  - name: dim_user
    description: This dimension includes all relevant info for users.

    columns:

      - name: user_id
        description: Primary key of this table     
        data_type: varchar
        constraints:
            - type: not_null
            - type: unique
            - type: primary_key
        tests:
          - unique
          - not_null

